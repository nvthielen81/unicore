#include <stdio.h>
void kbd_init()
{

    // Initialize the keyboard driver
    // This function would typically set up the hardware, configure interrupts, etc.
    // For now, we will just print a message to indicate that the keyboard driver is initialized.
    
    printf("Keyboard driver initialized.                    [ OK ] \n");
    
    // Additional initialization code can be added here as needed.
}